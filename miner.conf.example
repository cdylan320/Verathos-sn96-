# Verathos Miner Configuration
# Copy to miner.conf and edit, or run:  ./miner configure
#
#   cp miner.conf.example miner.conf

# Bittensor wallet (must exist in ~/.bittensor/wallets/)
WALLET_NAME="miner"
HOTKEY_NAME="default"

# Network: finney = mainnet (netuid 96), test = testnet (netuid 405)
NETWORK="finney"
NETUID=96

# Public HTTPS URL validators use to reach your miner (required on mainnet)
# Example: https://203.0.113.10:443  or  https://203.0.113.10:8443
ENDPOINT="https://YOUR_PUBLIC_IP:443"

# HTTPS reverse proxy (nginx) — setup with: ./miner https
HTTPS_PORT=443
BACKEND_PORT=8000

# Model: "auto" picks the best model for your GPU (recommended for L40 48GB)
MODEL_ID="auto"

# Process manager
PM2_NAME="miner"
USE_PM2=true

# Hugging Face token (optional) — higher download rate limits
# Get one at https://huggingface.co/settings/tokens
# export HF_TOKEN="hf_..."
